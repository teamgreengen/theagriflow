export default function LandingPage() {
  return (
    <main style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <section style={{ textAlign: 'center' }}>
        <h1>Agriflow Standalone Landing</h1>
        <p>This is a minimal standalone landing page to test dedicated deployments.</p>
        <p>If you can read this, the standalone deployment flow works.</p>
      </section>
    </main>
  )
}
