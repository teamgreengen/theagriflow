export default function LandingPage() {
  return (
    <main style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <section style={{ textAlign: 'center' }}>
        <h1>Agriflow Landing</h1>
        <p>This is a minimal landing page to test deployment flow.</p>
        <p>If you can read this, the deployment pipeline is functioning.</p>
      </section>
    </main>
  )
}
